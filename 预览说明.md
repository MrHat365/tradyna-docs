# 预览说明

## 开发预览

```bash
cd 使用文档
npm install
npm run docs:dev
```

默认地址：**http://127.0.0.1:5173/**

## 静态预览

```bash
npm run docs:build
npm run docs:preview
```

默认地址：**http://127.0.0.1:4173/**

## 更新截图

1. 仓库根目录启动终端：`set -a && source .env && set +a && bun run start`
2. `cd 使用文档 && npm run screenshots`

脚本从仓库根 `.env` 读取 `DOM_ACCESS_TOKEN` 仅用于自动登录，**不会**把真实令牌写入文档正文；截图中令牌输入框会打码。截图输出到 `docs/public/screenshots/`。
